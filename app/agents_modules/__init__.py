"""Independent agent implementations for the AI Co-Scientist system.

Each agent lives in its own module so collaborators can work independently.
The public compatibility imports remain available from ``app.agents``.
"""
